

export default function AdminCityInfo({
    infoContainer
}){
    return(
        <div
            id="popUpBackground"
        >
            {infoContainer(
                
            )}
        </div>
    )
}